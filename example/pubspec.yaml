name: example
description: A new Flutter project.
publish_to: 'none' 
version: 1.0.0+1

environment:
  sdk: ">=3.7.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter
  equatable: ^2.0.7
  collection: 
  cupertino_icons: ^1.0.8
  provider: ^6.1.4
  sheet:
    path: ../
  go_router: ^15.1.1

dev_dependencies:
  flutter_test:
    sdk: flutter
  analysis_lints: ^1.0.1

flutter:
  uses-material-design: true

  assets:
   - assets/
